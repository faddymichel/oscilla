export const scenario = {};

scenario .heading = '#scenario #play';
scenario .description = '';

export character = {};

for ( let i = 0, scale = 'sedrfjikolp;'; i < scale .length; i++ ) {

const character = scale [ i ];

scene [ character ] = {};
scene [ character ] .dialog = `#character #pitch ${ pitch }`;
scene [ character ] .action = `

}
