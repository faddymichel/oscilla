import { Instrument } from './Instrument.js';

export const descriptor = {};

descriptor .enumerable = true;

descriptor .value = function instrument () {

const osilla = this;
const symbol = Symbol ();
oscilla [ symbol ] = new Instrument ();

};
